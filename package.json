{
  "name": "cutedino-fly-connectome",
  "version": "1.0.0",
  "description": "80 real neurons from the MaleCNS fly connectome playing cute-dino",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve -l 4173 .",
    "channels": "node scripts/remap-channels.mjs",
    "train": "node scripts/train.mjs",
    "benchmark": "node scripts/benchmark.mjs",
    "test": "node scripts/verify-determinism.mjs && node scripts/verify-policy.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "dependencies": {}
}
